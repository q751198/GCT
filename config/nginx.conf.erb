server {
    listen <%= ENV["PORT"] %>;  # Heroku가 제공하는 PORT 사용
    root d.;                     # 현재 프로젝트 루트를 정적 파일 경로로 사용
    index index.html;           # 기본 파일

    location / {
        try_files $uri $uri/ =404;
    }
}
