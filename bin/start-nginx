#!/bin/sh

# Render nginx.conf.erb to nginx.conf using 'erb' command
erb config/nginx.conf.erb > config/nginx.conf

# Start NGINX using the rendered config file
nginx -g "daemon off;" -c config/nginx.conf