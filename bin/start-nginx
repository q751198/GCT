#!/bin/sh

# Debug: PORT 값 확인
echo "Using PORT: $PORT"

# nginx.conf.erb -> nginx.conf 렌더링
erb config/nginx.conf.erb > config/nginx.conf

# Debug: 렌더링된 nginx.conf 확인
echo "Rendered nginx.conf:"
cat config/nginx.conf

# NGINX 실행
nginx -g "daemon off;" -c config/nginx.conf